# Telegram-Group-Keeper-Bot
## if u add thia bot to your group in telegram than automatically it will filter the spam links and banned words you set in banned words file

Telegram group moderation bot.
Run: set TELEGRAM_TOKEN and ADMIN_ID env vars and `python bot.py`.
